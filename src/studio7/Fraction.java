package studio7;

public class Fraction {
	private double numerator;
	private double denominator; 
	public double sumofFraction(Fraction B) {
		double denominator;
		return productofFraction(null);
		
	}
	public double productofFraction(Fraction A) {
		 double newNum =this.numerator * A.numerator;
		 double newDen = this.denominator * A.denominator;
		 return newNum/newDen;
	
	}
}
